@model Movie

<form asp-controller="Home" asp-action="RedirectToEdit" method="get">
    <input type="hidden" asp-for="@Model.movieID" />
    <span class="card-text p-1">
        <button type="submit" class="btn btn-secondary">
            Edit
        </button>
    </span>
</form>