@model Movie

<form id="@Model.movieID" asp-controller="Home" asp-action="EditFilm" method="post">
    <input type="hidden" asp-for="movieID" />
    <span class="card-text p-1">
        <button type="submit" class="btn btn-secondary">
            Edit
        </button>
    </span>
</form>